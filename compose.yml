services:
  web:
    build:
      context: .
      target: runner
    image: yoga-flow:local
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
